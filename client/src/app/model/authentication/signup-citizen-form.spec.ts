import { SignupCitizenForm } from './signup-citizen-form';

describe('SignupCitizenForm', () => {
  it('should create an instance', () => {
    expect(new SignupCitizenForm()).toBeTruthy();
  });
});
